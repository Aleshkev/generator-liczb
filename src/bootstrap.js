import install from "@material-ui/styles/install";

install();

// "It is recommended to place the above code in a separate file (e.g. bootstrap.js) and to import it in your application's entry point (e.g. index.js).
// This ensures that the installation is executed before anything else, because ECMAScript imports are hoisted to the top of the module."
